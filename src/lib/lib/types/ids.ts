type SeasonId = number;
type ContractId = `${number}-${number}`;
type ContractYearId = `${ContractId}-${number}`;
type PlayerId = number;
type DraftPickId = number;
type StaffId = `STA${number}`;
type StaffTenureId = `${StaffId}-${number}`;
type TradeId = `${number}`;
